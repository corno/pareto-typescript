#!/usr/bin/env -S node --enable-source-maps

import * as p_h from 'pareto-host-nodejs/index'

//temporary placeholder, needed because pdt wants to set permissions.
//the solution is that the files in /bin/* get integrated into this file, using command line arguments to determine which command to run.
